# Copy this file to ".streamlit/secrets.toml" and fill in your key.
# Get a free key at https://www.zip.tax/ (100 calls to start).
# secrets.toml is gitignored so your key never gets committed.

ZIPTAX_API_KEY = "your_zip_tax_api_key_here"
